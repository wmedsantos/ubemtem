{
  "name": "ubemtem",
  "version": "1.0.0",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",    
    "postbuild": "rmdir /s /q docs && mkdir docs && xcopy _site docs /E /I /Y && xcopy src\\admin\\config.yml docs\\admin\\config.yml /Y"
    },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0"
  }
}